<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<OutputType>Exe</OutputType>
		<TargetFramework>net10.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<RootNamespace>ParseTimeshiftSnapshots</RootNamespace>
		<AssemblyName>ParseTimeshiftSnapshots</AssemblyName>
		<Description>Parses timeshift snapshots and returns a list of backups that should be deleted.</Description>
	</PropertyGroup>
	<ItemGroup>
	</ItemGroup>
</Project>
