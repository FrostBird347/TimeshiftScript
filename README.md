# TimeshiftScript
My custom backup script for timeshift.

Note that the script must be run as root, needs direct access to the timeshift snapshots folder and can't handle multiple backup disks or btrfs snapshots.
